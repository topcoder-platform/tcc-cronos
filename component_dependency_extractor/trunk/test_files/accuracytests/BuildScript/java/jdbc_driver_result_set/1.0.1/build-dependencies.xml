<project name="Dependency Import" default="dist" basedir=".">
    <!-- property file defining the component's dependencies -->

    <!-- Properties used in compile_targets ANT target -->
    <property name="component.target" value="1.5"/>
    <property name="component.bootclasspath" value="${java_1_5_bootclasspath}"/>

    <!-- TCS dependencies -->
    <property name="jdbc_driver_io_interfaces.jar" value="${tcs_libdir}/jdbc_driver_io_interfaces/1.0.2/jdbc_driver_io_interfaces.jar" />
    <property name="jdbc_driver_data_types.jar" value="${tcs_libdir}/jdbc_driver_data_types/1.0.0/jdbc_driver_data_types.jar" />

    <!-- 3rd party dependencies -->
    <property name="junit.jar" value="${ext_libdir}/junit/3.8.2/junit.jar"/>

    <path id="component.tcs-dependencies">
        <pathelement location="${jdbc_driver_io_interfaces.jar}" />
        <pathelement location="${jdbc_driver_data_types.jar}" />
    </path>

    <path id="component.3rdParty-dependencies"/>

    <path id="component.test.3rdParty-dependencies">
        <pathelement location="${junit.jar}"/>
    </path>

</project>
