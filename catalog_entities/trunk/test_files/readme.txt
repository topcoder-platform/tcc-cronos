All requirement of RS has been achieved

1. Add the source code for implementing the new requirements, as well as unit tests covering them.
2. Update the mapping file (see ./test_files/META-INF/catalog_entities.hbm.xml) with the new entity.
3. Update the class diagram(see the tcuml under ./doc/) and component specification to add the new entity.
4. Update the DB schema and scripts to add the new tables(see *.sql and *.DLL under ./test_files/sql/)

Updated sigment of CS is marked blue

Good luck!
TCSDEV