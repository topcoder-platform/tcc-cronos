<project name="Override Import" basedir=".">	
	
	  <property name="war.dir" value="photodemo.war" />
	  <property name="deploy.dir" value="D:/jboss-5.1.0.GA/server/default/deploy" />

    <target name="deploy" depends="compile, compile_tests">
        <copy todir="${war.dir}/WEB-INF/classes">
            <fileset dir="${build_classdir}"/>
            <fileset dir="${build_testclassdir}"/>
        </copy>
    	<copy todir="${deploy.dir}" >
           <fileset dir="${testfiles}">
             <include name="${war.dir}/**" />
           </fileset>
        </copy>

    </target>

    <target name="undeploy">
    	<delete dir="${deploy.dir}/${war.dir}"/>
    </target>
</project>