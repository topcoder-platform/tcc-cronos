<project name="Dependency Import" default="dist" basedir=".">
    <!-- property file defining the component's dependencies -->

    <!-- Properties used in compile_targets ANT target for JDK version x -->
    <property name="component.target" value="1.x"/>
    <property name="component.bootclasspath" value="${java_1_x_bootclasspath}"/>

    <!-- TCS dependencies -->
    <property name="base_exception.jar" value="lib/base_exception.jar"/>
    <property name="configuration_api.jar" value="lib/configuration_api.jar"/>
    <property name="object_factory_configuration_api_plugin.jar" value="lib/object_factory_configuration_api_plugin.jar"/>
    <property name="predictor_framework.jar" value="lib/predictor_framework.jar"/>

    <!-- 3rd party dependencies -->
    <property name="junit.jar" value="lib/junit.jar"/>

    <path id="component.tcs-dependencies">
        <pathelement location="${base_exception.jar}"/>
        <pathelement location="${configuration_api.jar}"/>
        <pathelement location="${object_factory_configuration_api_plugin.jar}"/>
        <pathelement location="${predictor_framework.jar}"/>
    </path>

    <path id="component.3rdParty-dependencies"/>

    <path id="component.test.3rdParty-dependencies">
        <pathelement location="${junit.jar}"/>
    </path>

</project>