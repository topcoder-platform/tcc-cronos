Important:

TC Registration Framework 1.0 contains a class with the same name and
in the same package as the one developed in this competition, therefore,
make sure the registration_framework.jar file is loaded last during 
compilation.
