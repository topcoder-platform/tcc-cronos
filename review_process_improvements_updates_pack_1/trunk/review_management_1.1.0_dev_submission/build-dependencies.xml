<project name="Dependency Import" default="dist" basedir=".">
    <!-- property file defining the component's dependencies -->

    <!-- Properties used in compile_targets ANT target for JDK version x -->
    <property name="component.target" value="1.4"/>
    <property name="component.bootclasspath" value="${java_1_4_bootclasspath}"/>

   <!-- TCS dependencies -->

    <!-- 3rd party dependencies -->
    <property name="junit.jar" value="${ext_libdir}/junit/junit-3.8.jar"/>
    <property name="search_builder.jar" value="${tcs_libdir}/search_builder/1.4.1/search_builder.jar"/>

<property name="configuration_manager.jar" value="${tcs_libdir}/configuration_manager/2.1.5/configuration_manager.jar"/>

<property name="base_exception.jar" value="${tcs_libdir}/base_exception/1.0/base_exception.jar"/>
<property name="review_data_structure.jar" value="${tcs_libdir}/review_data_structure/1.1/review_data_structure.jar"/>
                             
    <path id="component.tcs-dependencies">
	  <pathelement location="${search_builder.jar}"/>

<pathelement location="${configuration_manager.jar}"/>

<pathelement location="${base_exception.jar}"/>
<pathelement location="${review_data_structure.jar}"/>

</path>
    <path id="component.3rdParty-dependencies">

    </path>
    <path id="component.test.3rdParty-dependencies">

          <pathelement location="${junit.jar}"/>

    </path>

</project>